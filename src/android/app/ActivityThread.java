package android.app;

/**
 * Minimal compile-time stub for hidden Android internal class ActivityThread.
 * This allows javac to succeed. Keep implementation empty — the real class on
 * the device will be used at runtime.
 */
public final class ActivityThread {
    // Called from MetaServer: ActivityThread.initializeMainlineModules();
    public static void initializeMainlineModules() {
        // no-op stub for compilation
    }
}